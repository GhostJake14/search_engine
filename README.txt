Start Elasticsearch node by running elasticsearch.bat file from bin folder of Elasticsearch.
npm install - to install node dependencies from package.json
node server.js - to start the server on port defined in .env file
Go to the browser and open http://localhost:3000/ (Port 3000 is defined in .env folder)